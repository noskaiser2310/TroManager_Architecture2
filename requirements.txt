# -----------------------------------------------------------
# TroManager — Runtime dependencies (production)
# -----------------------------------------------------------
# Cài: pip install -r requirements.txt
# Cài dev: pip install -r requirements.txt -r requirements-dev.txt

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.2.0

# LLM (OpenAI-compatible endpoint — dùng cho Google Gemini, OpenAI, v.v.)
openai>=1.0.0

# LangChain (chỉ dùng @tool decorator)
langchain>=0.1.0

# HTTP + Scheduling + Utils
httpx>=0.25.0
apscheduler>=3.10.0
python-dotenv>=1.0.0
pyyaml>=6.0
tenacity>=8.2.0
aiofiles>=23.2.0
